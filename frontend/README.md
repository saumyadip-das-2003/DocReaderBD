# DocReader BD Frontend

Next.js 14 app for building templates and extracting fields.

## Setup
```bash
npm install
npm run dev
```

Copy `.env.local.example` to `.env.local` and update `NEXT_PUBLIC_API_URL` if your backend runs somewhere other than `http://localhost:8000`.
